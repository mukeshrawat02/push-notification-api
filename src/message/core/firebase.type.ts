export type ProjectConfiguration = {
    id: string,
    serverKey: string,
    databaseURL: string,
    serviceAccount: any,
};

export type ProjectData = {
    id: string,
    ref: any,
    serverKey: string,
};